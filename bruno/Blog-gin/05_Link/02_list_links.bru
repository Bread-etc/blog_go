meta {
  name: 02 List Links
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/links
  body: none
  auth: none
}

tests {
  test("link list returns array", function () {
    expect(res.status).to.equal(200);
    expect(res.body.code).to.equal(200);
    expect(res.body.errorCode).to.equal("");
    expect(res.body.data).to.be.an("array");
    expect(res.body.data.some((item) => item.id === bru.getVar("linkId"))).to.equal(true);
  });
}

