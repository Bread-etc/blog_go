meta {
  name: 15 Cleanup Category
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/api/categories/{{postCategoryId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("post category cleaned", function () {
    expect(res.status).to.equal(200);
    expect(res.body.code).to.equal(200);
    expect(res.body.errorCode).to.equal("");
  });
}

