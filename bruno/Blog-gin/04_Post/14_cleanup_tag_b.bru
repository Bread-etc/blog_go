meta {
  name: 14 Cleanup Tag B
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/api/tags/{{postTagIdB}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("post tag b cleaned", function () {
    expect(res.status).to.equal(200);
    expect(res.body.code).to.equal(200);
    expect(res.body.errorCode).to.equal("");
  });
}

