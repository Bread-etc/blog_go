meta {
  name: 13 Cleanup Tag A
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/api/tags/{{postTagIdA}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("post tag a cleaned", function () {
    expect(res.status).to.equal(200);
    expect(res.body.code).to.equal(200);
    expect(res.body.errorCode).to.equal("");
  });
}

