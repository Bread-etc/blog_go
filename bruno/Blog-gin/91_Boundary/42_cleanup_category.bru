meta {
  name: 42 Cleanup Category
  type: http
  seq: 42
}

delete {
  url: {{baseUrl}}/api/categories/{{boundaryCategoryId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("boundary category cleaned", function () {
    expect(res.status).to.equal(200);
    expect(res.body.code).to.equal(200);
    expect(res.body.errorCode).to.equal("");
  });
}
