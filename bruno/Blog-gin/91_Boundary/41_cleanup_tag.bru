meta {
  name: 41 Cleanup Tag
  type: http
  seq: 41
}

delete {
  url: {{baseUrl}}/api/tags/{{boundaryTagId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("boundary tag cleaned", function () {
    expect(res.status).to.equal(200);
    expect(res.body.code).to.equal(200);
    expect(res.body.errorCode).to.equal("");
  });
}
